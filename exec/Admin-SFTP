#!/bin/bash
echo -ne "\033[38;5;4mumask: \033[0m"
read umask
sshfs FTN-Server:/ /Volumes/Root-SFTP \
    -o reconnect,ServerAliveInterval=15,ServerAliveCountMax=10 \
    -o volname="Server - SFTP" \
    -o volicon="$(dirname "$(realpath "$0")")/.icon.tiff" \
    -o umask=$umask
exit
